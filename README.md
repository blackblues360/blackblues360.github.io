# BLACK BLUES

BLACK BLUES 是一个部署在 GitHub Pages 上的纯静态个人主页。

## 结构

- `index.html`：首页内容和页面结构。
- `styles.css`：全站样式。
- `script.js`：移动端导航和进入视口动画。
- `posts/`：静态文章页。
- `CNAME`：GitHub Pages 自定义域名配置。

## 本地查看

直接在浏览器打开 `index.html` 即可预览。也可以使用任意静态文件服务器从仓库根目录启动。
